<?xml version="1.0" encoding="utf-8"?>
    	<manifest xmlns:android="http://schemas.android.com/apk/res/android"
   	    package="com.boonstudio.optimusprime.loool" >

   	    <application
   	        android:allowBackup="true"
   	        android:icon="@drawable/ic_launcher"
            android:label="@string/app_name"
            android:theme="@style/AppTheme" >

            <!-- Splash Activity -->
  	        <activity
    	            android:name="com.boonstudio.optimusprime.loool.SplashActivity"
    	            android:label="@string/app_name"
    	            android:screenOrientation="portrait"
    	            android:noHistory="true" >
               <intent-filter>
                 <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
               </intent-filter>
   	        </activity>

    	        <!-- Main Activity -->
    	        <activity
    	            android:name="com.boonstudio.optimusprime.loool.Main"
    	            android:label="@string/app_name" >
   	        </activity>
            <!-- Megatron Activity -->
            <activity
                android:name="com.boonstudio.optimusprime.loool.Megatron"
                android:label="@string/app_name" >
            </activity>


    	    </application>

    	</manifest>